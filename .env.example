# Universal Downloader v3.0 — Environment Variables
# Untuk local dev, copy ke .env
# Untuk Vercel, set di Dashboard → Settings → Environment Variables

# URL API setelah deploy ke Vercel (untuk plugin WA bot)
# DOWNLOADER_API=https://nama-project-kamu.vercel.app

# Port untuk local dev
PORT=3155
